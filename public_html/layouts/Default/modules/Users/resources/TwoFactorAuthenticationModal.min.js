'use strict';

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};

var createClass = function () {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }

  return function (Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();

window.Base_TwoFactorAuthenticationModal_JS=function(){function _class(){classCallCheck(this,_class);}return createClass(_class,[{key:'registerSubmitEvent',/**
	 * Function to register the click event for generate button
	 */value:function registerSubmitEvent(){var methods=this.container.find('#methods'),code=this.container.find('#user_code'),secret=this.container.find('#secret'),form=this.container.find('form');form.validationEngine(app.validationEngineOptions),this.container.find('.js-modal__save').on('click',function(){form.validationEngine('validate')&&AppConnector.request({module:'Users',action:'TwoFactorAuthenticationModal',method:methods.val(),code:code.val(),secret:secret.val()}).done(function(responseData){responseData.result&&(app.showNotify({text:responseData.result,type:'success'}),app.hideModalWindow());}).fail(function(error,title){app.showNotify({text:title,type:'error'});});});}/**
	 * Register modal events.
	 * @param {jQuery} modalContainer
	 */},{key:'registerEvents',value:function registerEvents(modalContainer){this.container=modalContainer,this.registerSubmitEvent();}}]),_class}();
//# sourceMappingURL=TwoFactorAuthenticationModal.min.js.map
