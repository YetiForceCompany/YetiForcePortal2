'use strict';

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};

var createClass = function () {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }

  return function (Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();

window.Base_PasswordChangeModal_JS=function(){function _class(){classCallCheck(this,_class);}return createClass(_class,[{key:'registerSubmitEvent',/**
	 * Function to register the submit event of form.
	 */value:function registerSubmitEvent(){var formElement=this.container.find('form');formElement.validationEngine(app.validationEngineOptions),formElement.on('submit',function(e){if(e.preventDefault(),formElement.validationEngine('showPrompt',resultOfValidation,'','topLeft',!0),!0===formElement.validationEngine('validate')){var formData=formElement.serializeFormData();AppConnector.request(formData).done(function(data){!0===data.success&&app.showNotify({text:data.result,type:'success'});});}});}/**
	 * Register modal events.
	 * @param {jQuery} modalContainer
	 */},{key:'registerEvents',value:function registerEvents(modalContainer){this.container=modalContainer,this.registerSubmitEvent();}}]),_class}();
//# sourceMappingURL=PasswordChangeModal.min.js.map
