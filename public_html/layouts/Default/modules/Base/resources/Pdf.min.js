'use strict';

/* {[The file is published on the basis of YetiForce Public License 3.0 that can be found in the following directory: licenses/LicenseEN.txt or yetiforce.com]} */$.Class('Base_Pdf_Js',{},{/**
		 * Function to register the click event for generate button
		 * @param {jQuery} container
		 */registerSubmitEvent:function registerSubmitEvent(container){container.find('#generatePdf').on('click',function(){var templateIds=[];container.find('[type="checkbox"].js-template').each(function(){$(this).is(':checked')&&templateIds.push($(this).val());}),container.find('[name="templates"]').val(JSON.stringify(templateIds));});},/**
		 * Validate submit button
		 * @param   {jQuery}  container
		 */validateSubmit:function validateSubmit(container){var disabled=!0;container.find('[type="checkbox"].js-template').each(function(){if($(this).prop('checked'))return disabled=!1,!1}),container.find('#generatePdf').attr('disabled',disabled);},/**
		 * Register validate submit
		 * @param   {jQuery}  container
		 */registerValidateSubmit:function registerValidateSubmit(container){var _this=this;this.validateSubmit(container),container.find('[type="checkbox"].js-template').on('change',function(){_this.validateSubmit(container);});},/**
		 * Register all events in view
		 * @param {jQuery} container
		 */registerEvents:function registerEvents(container){this.registerSubmitEvent(container),this.registerValidateSubmit(container);}});
//# sourceMappingURL=Pdf.min.js.map
