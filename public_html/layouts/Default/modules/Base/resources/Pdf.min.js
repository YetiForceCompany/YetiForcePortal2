'use strict';

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};

var createClass = function () {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }

  return function (Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();

window.Base_Pdf_JS=function(){function _class(){classCallCheck(this,_class);}return createClass(_class,[{key:'registerSubmitEvent',/**
	 * Function to register the click event for generate button
	 */value:function registerSubmitEvent(){var _this=this;this.container.find('#pdfExportModal').on('submit',function(){var templateIds=[];_this.container.find('[type="checkbox"].js-template').each(function(){$(this).is(':checked')&&templateIds.push($(this).val());}),_this.container.find('[name="templates"]').val(JSON.stringify(templateIds));});}/**
	 * Validate submit button
	 */},{key:'validateSubmit',value:function validateSubmit(){var disabled=!0;this.container.find('[type="checkbox"].js-template').each(function(){if($(this).prop('checked'))return disabled=!1,!1}),this.container.find('#generatePdf').attr('disabled',disabled);}/**
	 * Register validate submit
	 */},{key:'registerValidateSubmit',value:function registerValidateSubmit(){var _this2=this;this.validateSubmit(),this.container.find('[type="checkbox"].js-template').on('change',function(){_this2.validateSubmit();});}/**
	 * Register modal events.
	 * @param {jQuery} modalContainer
	 */},{key:'registerEvents',value:function registerEvents(modalContainer){this.container=modalContainer,this.registerSubmitEvent(),this.registerValidateSubmit();}}]),_class}();
//# sourceMappingURL=Pdf.min.js.map
