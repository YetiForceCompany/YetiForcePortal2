'use strict';

var classCallCheck = function (instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
};

var createClass = function () {
  function defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }

  return function (Constructor, protoProps, staticProps) {
    if (protoProps) defineProperties(Constructor.prototype, protoProps);
    if (staticProps) defineProperties(Constructor, staticProps);
    return Constructor;
  };
}();

window.Base_ListView_Js=function(){function _class(){classCallCheck(this,_class);}return createClass(_class,[{key:'registerDataTable',/**
	 * Register data table
	 */value:function registerDataTable(){var _this=this;this.dataTable=app.registerDataTables(this.table,{order:[],processing:!0,serverSide:!0,searching:!1,orderCellsTop:!0,ajax:{url:'index.php',type:'POST',data:function data(_data){$.extend(_data,_this.listForm.serializeFormData());}}}),this.listForm.find('input').on('change',function(){_this.dataTable.ajax.reload();});}/**
	 * Register record events
	 */},{key:'registerRecordEvents',value:function registerRecordEvents(){}// table.$('.deleteRecordButton').on('click', function (e) {
// 	e.stopPropagation();
// 	e.preventDefault();
// 	var element = jQuery(e.currentTarget);
// 	AppConnector.request({
// 		data: {},
// 		url: element.data('url')
// 	}).then(
// 		function (data) {
// 			if (data.result) {
// 				table.row(element.closest('tr')).remove().draw();
// 			}
// 		},
// 		function (e, err) {
// 			console.log([e, err]);
// 		}
// 	);
// });
// table.$('.listViewEntries tbody tr').on('click', function (e) {
// 	e.stopPropagation();
// 	e.preventDefault();
// 	if ($.contains(jQuery(e.currentTarget).find('td:first-child').get(0), e.target)) {
// 		return;
// 	}
// 	jQuery(e.currentTarget).find('.detailLink').trigger('click');
// });
// var listViewContentDiv = this.getListViewContentContainer();
// 	listViewContentDiv.on('click', '.deleteRecordButton', function (e) {
// 		var elem = jQuery(e.currentTarget);
// 		var recordId = elem.closest('tr').data('id');
// 		Vtiger_List_Js.deleteRecord(recordId);
// 		e.stopPropagation();
// 	});
/**
	 * Register modal events.
	 */},{key:'registerEvents',value:function registerEvents(){this.listForm=$('.js-form-container'),this.table=this.listForm.find('.js-list-view-table'),this.registerDataTable();}}]),_class}();
//# sourceMappingURL=ListView.min.js.map
